---
const pathname = new URL(Astro.request.url).pathname;
const currentPath = pathname.slice(1); // remove first "/"
const currentYear = new Date().getFullYear();
import { Image } from 'astro:assets';
import logoFooter from '../img/logo-footer.svg';
import gbp from '../img/gbp-mobile.webp';
import '../styles/form.scss';
import '../styles/footer.scss';
const turnstileSiteKey = import.meta.env.PUBLIC_TURNSTILE_SITE_KEY || '';
const apiPath = `${import.meta.env.BASE_URL}api/tech-review/`;
---

<section id="contact">
	<div class="container top btm">
		<div class="flex padding-x align-middle">
			<div class="cell small-18 medium-10">
				<div id="form-container trig-fade-right enable-trig">
					<h2>Contact</h2>
					<Image src={gbp} class="contact-mobile trig-target" alt="TradesAdmin Contact" />
					<p class="margin-btm-med intro semibold">For a technical review of your website and search results, fill out and submit the form below.</p>

					<form method="POST" id="form" data-api-path={apiPath} class="needs-validation" novalidate>
						<div>
							<label for="company" class="visually-hidden">Company Name</label>
							<input type="text" name="company" id="company" placeholder="Company Name *" required />
							<span class="helperText">Company Name</span>
							<div class="empty-feedback invalid-feedback">Please provide your company name</div>
						</div>

						<div>
							<label for="url" class="visually-hidden">URL</label>
							<input type="text" name="url" id="url" placeholder="URL *" required inputmode="url" autocomplete="url" />
							<span class="helperText">URL</span>
							<div class="empty-feedback invalid-feedback">Please provide your URL</div>
							<div class="invalid-feedback">Please provide a valid URL (example.com, http://example.com, or https://example.com)</div>
						</div>

						<div>
							<label for="email_address" class="visually-hidden">Email</label>
							<input type="email" name="email" id="email_address" placeholder="Email *" required pattern="^[^\s@]+@[^\s@]+\.[^\s@]{2,}$" />
							<span class="helperText">Email</span>
							<div class="empty-feedback">Please provide your email address</div>
							<div class="invalid-feedback">Please provide a valid email address</div>
						</div>

						<div>
							<label for="phone" class="visually-hidden">Phone</label>
							<input type="tel" name="phone" id="phone" placeholder="Phone" />
							<span class="helperText">Phone</span>
						</div>

						<div>
							<label for="details" class="visually-hidden">Details</label>
							<textarea rows="10" name="details" id="details" placeholder="Questions & Comments"></textarea>
							<span class="helperText">Details</span>
						</div>

						<div class="margin-top-sm">
							<div id="turnstile-container" class="cf-turnstile" data-sitekey={turnstileSiteKey}></div>
						</div>

						<p id="result" style="display:none;"></p>
						<button type="submit" class="btn arrow">SUBMIT</button>
					</form>

					<script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer></script>
				</div>
			</div>
			<div class="cell small-18 medium-8 trig-fade-right enable-trig contact-desktop">
				<Image src={gbp} class="trig-target" alt="TradesAdmin Contact" />
			</div>
		</div>
	</div>
</section>

<footer id="site-footer">
	<div class="container top">
		<div class="flex padding-x">
			<div class="cell small-18">
				<Image class="footer-logo" src={logoFooter} alt="TradesAdmin Logo" />
			</div>
		</div>

		<div class="flex padding-x">
			<div class="cell small-18 medium-10">
				<div class="footer-brand">
					<div class="footer-description">
						<p>
							<span class="semibold">TradesAdmin</span> supports small service businesses across the GTA with technical website corrections and accurate local search configuration to ensure contractor websites
							function reliably and are correctly represented in search results.
						</p>
					</div>
				</div>
			</div>

			<!-- Services Section -->
			<div class="cell small-18 medium-8">
				<div class="footer-section">
					<ul class="footer-links">
						<li>
							<a class={currentPath === 'services/' ? 'active' : ''} href="#services" role="link" aria-label="link to SERVICES section">SERVICES</a>
						</li>
						<li>
							<a class={currentPath === 'services/' ? 'active' : ''} href="#pricing" role="link" aria-label="link to pricing section">PRICING</a>
						</li>
						<li>
							<a class={currentPath === 'services/' ? 'active' : ''} href="#process" role="link" aria-label="link to Process section">Process</a>
						</li>
						<li>
							<a class={currentPath === 'services/' ? 'active' : ''} href="#testimonials" role="link" aria-label="link to TESTIMONIALS section">TESTIMONIALS</a>
						</li>
						<li>
							<a class={currentPath === 'contact/' ? 'active' : ''} href="#contact" role="link" aria-label="link to contact section">CONTACT</a>
						</li>
					</ul>

					<div class="contact-info">
						<div class="contact-item">
							<svg width="16" height="16" fill="currentColor" viewBox="0 0 24 24">
								<path d="M20 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"></path>
							</svg>
							<a href="mailto:hello@tradesadmin.ca">hello@TradesAdmin.ca</a>
						</div>
						<div class="contact-item">
							<svg width="16" height="16" fill="currentColor" viewBox="0 0 24 24">
								<path
									d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"
								></path>
							</svg>
							<a href="tel:4169861059">416 986 1059</a>
						</div>
						<div class="contact-item">
							<svg width="16" height="16" fill="currentColor" viewBox="0 0 24 24">
								<path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"></path>
							</svg>
							<span>Toronto, Canada</span>
						</div>
					</div>
				</div>
			</div>
		</div>

		<!-- Footer Bottom -->
		<div class="footer-bottom">
			<div class="flex padding-x align-middle">
				<div class="cell small-18">
					<div class="footer-bottom-content">
						<p class="copyright">© {currentYear} TradesAdmin.ca. All rights reserved.</p>
						<!-- <div class="footer-bottom-links">
							<a href="/terms/">Terms</a>
							<a href="/privacy/">Privacy</a>
							<a href="/cookies/">Cookies</a>
						</div> -->
					</div>
				</div>
			</div>
		</div>
	</div>
</footer>

<script src="../scripts/trig.js"></script>
<script src="../scripts/scripts.js"></script>
<!-- <script src="../scripts/lenis.js"></script> -->

<script is:inline>
	document.addEventListener('astro:page-load', () => {
		// Remove accidental duplicates if present
		const widgets = document.querySelectorAll('.cf-turnstile');
		if (widgets.length > 1) {
			widgets.forEach((el, i) => {
				if (i > 0) el.remove();
			});
		}
	});
</script>

<script is:inline>
	document.addEventListener('astro:page-load', () => {
		const form = document.getElementById('form');
		const result = document.getElementById('result');
		const urlInput = document.getElementById('url');

		if (!form || !result || !urlInput) return;

		let submitted = false;

		function normalizeUrl(value) {
			const raw = String(value || '').trim();
			if (!raw) return '';
			return /^https?:\/\//i.test(raw) ? raw : `https://${raw}`;
		}

		function isValidUrlLoose(value) {
			const normalized = normalizeUrl(value);
			if (!normalized) return false;
			try {
				const u = new URL(normalized);
				return Boolean(u.hostname && u.hostname.includes('.'));
			} catch {
				return false;
			}
		}

		function updateUrlValidity() {
			const raw = urlInput.value.trim();
			if (!raw) {
				urlInput.setCustomValidity(''); // required handles empty
				return;
			}
			urlInput.setCustomValidity(isValidUrlLoose(raw) ? '' : 'Please provide a valid URL');
		}

		function getTurnstileToken() {
			const fieldToken = form.querySelector('input[name="cf-turnstile-response"]')?.value?.trim() || form.querySelector('textarea[name="cf-turnstile-response"]')?.value?.trim() || '';

			if (fieldToken) return fieldToken;

			if (window.turnstile && typeof window.turnstile.getResponse === 'function') {
				try {
					return String(window.turnstile.getResponse() || '').trim();
				} catch {
					return '';
				}
			}

			return '';
		}

		function isFormField(el) {
			return el instanceof HTMLInputElement || el instanceof HTMLTextAreaElement || el instanceof HTMLSelectElement;
		}

		function updateFieldState(field, force = false) {
			if (!isFormField(field)) return;

			if (field.id === 'url') updateUrlValidity();

			const touched = field.dataset.touched === 'true';
			if (!force && !touched) {
				field.classList.remove('is-valid', 'is-invalid');
				return;
			}

			field.classList.toggle('is-invalid', !field.checkValidity());
			field.classList.toggle('is-valid', field.checkValidity());
		}

		function validateAllFields() {
			form.classList.add('was-validated');
			updateUrlValidity();

			const fields = form.querySelectorAll('input, textarea, select');
			fields.forEach((f) => updateFieldState(f, true));

			return form.checkValidity();
		}

		async function submitForm() {
			const formData = new FormData(form);
			const endpoint = form.dataset.apiPath || '/api/tech-review/';

			const payload = {
				company: String(formData.get('company') || '').trim(),
				email: String(formData.get('email') || '').trim(),
				url: normalizeUrl(String(formData.get('url') || '').trim()),
				phone: String(formData.get('phone') || '').trim(),
				message: String(formData.get('details') || formData.get('message') || '').trim(),
				turnstileToken: getTurnstileToken(),
			};

			result.style.display = 'block';
			result.textContent = 'Sending...';

			try {
				const response = await fetch(endpoint, {
					method: 'POST',
					headers: { 'Content-Type': 'application/json' },
					body: JSON.stringify(payload),
				});

				const text = await response.text();
				console.log('API status/body:', response.status, text);
				let data = {};
				if (text) {
					try {
						data = JSON.parse(text);
					} catch {
						data = {};
					}
				}

				if (!response.ok) {
					const serverMessage = data?.error || text || (response.status >= 500 ? 'Server error. Please try again shortly.' : `Request failed (${response.status})`);
					throw new Error(serverMessage);
				}

				result.textContent = data?.message || 'Thanks. Your request was submitted.';

				form.reset();
				form.classList.remove('was-validated');
				form.querySelectorAll('.is-valid, .is-invalid').forEach((el) => {
					el.classList.remove('is-valid', 'is-invalid');
				});
				form.querySelectorAll('[data-touched]').forEach((el) => {
					el.removeAttribute('data-touched');
				});
				urlInput.setCustomValidity('');
				submitted = false;

				if (window.turnstile) window.turnstile.reset();
			} catch (err) {
				result.textContent = err instanceof Error ? err.message : 'Something went wrong. Please try again.';
			}
		}

		form.addEventListener(
			'blur',
			(e) => {
				const field = e.target;
				if (!isFormField(field)) return;
				field.dataset.touched = 'true';
				updateFieldState(field, submitted);
			},
			true,
		);

		form.addEventListener('input', (e) => {
			const field = e.target;
			if (!isFormField(field)) return;
			updateFieldState(field, submitted);
		});

		form.addEventListener('submit', (e) => {
			e.preventDefault();
			submitted = true;

			if (!validateAllFields()) {
				form.querySelector(':invalid')?.focus();
				return;
			}

			if (!getTurnstileToken()) {
				result.style.display = 'block';
				result.textContent = 'Please complete the verification.';
				return;
			}

			submitForm();
		});
	});
</script>
